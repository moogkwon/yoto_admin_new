import { cities, states } from './cities'
import { getAge, getMessageError, removeNonNumber, removeLeadingSpaces } from './utils'
import { required } from './Regex'
export {
  cities,
  states,
  getAge,
  getMessageError,
  removeNonNumber,
  removeLeadingSpaces,
  required
}
